<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Formulario del Docente</title>
</head>
<body class="container">
    <h1 class="text-center mt-5">Formulario del Docente</h1>

    <!-- Mostrar formulario -->
    <% if (!submitted) { %>
        <form action="/calculate" method="get" class="mt-4">
            <div class="mb-3">
                <label for="level" class="form-label">Nivel</label>
                <select name="level" id="level" class="form-select" required>
                    <option value="1">1</option>
                    <option value="2">2</option>
                    <option value="3">3</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="basePay" class="form-label">Pago Base</label>
                <input type="number" name="basePay" id="basePay" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="hours" class="form-label">Horas Impartidas</label>
                <input type="number" name="hours" id="hours" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="children" class="form-label">Número de Hijos</label>
                <select name="children" id="children" class="form-select" required>
                    <option value="1">1</option>
                    <option value="2">2</option>
                    <option value="3">3</option>
                    <option value="4">4</option>
                    <option value="5">5</option>
                    <option value="6">6+</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Enviar</button>
        </form>
    <% } else { %>
        <h2 class="text-center mt-5">Resultados</h2>
        <div class="mt-4">
            <p><strong>Pago Total:</strong> $<%= totalPayment %></p>
            <p><strong>Impuestos:</strong> $<%= tax %></p>
            <p><strong>Bono por Hijos:</strong> $<%= bonus %></p>
        </div>
        <a href="/pracRecu" class="btn btn-secondary mt-4">Volver</a>
    <% } %>
</body>
</html>
